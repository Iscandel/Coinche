#pragma once

#include "GameState.h"

#include "Gaia/Gaia.h"

class SBidding : public GameState
{
public:
	SBidding(void);
	~SBidding(void);

	void init();

		///////////////////////////////////////////////////////////////////////////
	/// Pauses the game state.
	///////////////////////////////////////////////////////////////////////////
	virtual void pause() {}

	///////////////////////////////////////////////////////////////////////////
	/// Resumes the game state.
	///////////////////////////////////////////////////////////////////////////
	virtual void resume() {}

	///////////////////////////////////////////////////////////////////////////
	/// Updates the game state.
	///////////////////////////////////////////////////////////////////////////
	virtual void update();

	void affectPlayerPositionToSouth();

	virtual void processMessage(const Message& msg);

	bool catchEvent(const sf::Event& ev);

	void onValider(gaia::MouseEvent& ev);

	void onPasser(gaia::MouseEvent& ev);

protected:
	std::vector<int> myPlayersReady;
};

